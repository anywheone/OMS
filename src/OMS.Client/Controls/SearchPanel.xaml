<UserControl x:Class="OMS.Client.Controls.SearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Border Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 検索ボックス -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding SearchText, ElementName=Root, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         Padding="10,8"
                         FontSize="14">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="🔍 検索..." Foreground="Gray" Margin="10,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="1"
                        Content="検索"
                        Padding="20,8"
                        Margin="10,0,0,0"
                        Command="{Binding SearchCommand, ElementName=Root}"/>
            </Grid>

            <!-- フィルターオプション（オプション） -->
            <StackPanel Grid.Row="1" Margin="0,10,0,0"
                        Visibility="{Binding ShowFilters, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="フィルター" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <ContentPresenter Content="{Binding FilterContent, ElementName=Root}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
