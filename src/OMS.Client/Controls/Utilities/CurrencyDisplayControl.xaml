<UserControl x:Class="OMS.Client.Controls.Utilities.CurrencyDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="30" d:DesignWidth="150"
             x:Name="root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 通貨記号 -->
        <TextBlock Grid.Column="0"
                   Text="{Binding CurrencySymbol, ElementName=root}"
                   FontSize="{Binding FontSize, ElementName=root}"
                   FontWeight="{Binding FontWeight, ElementName=root}"
                   Foreground="{Binding ComputedForeground, ElementName=root}"
                   Visibility="{Binding ShowCurrencySymbol, ElementName=root,
                              Converter={StaticResource BooleanToVisibilityConverter}}"
                   Margin="0,0,4,0"
                   VerticalAlignment="Center"/>

        <!-- 金額 -->
        <TextBlock Grid.Column="1"
                   Text="{Binding FormattedValue, ElementName=root}"
                   FontSize="{Binding FontSize, ElementName=root}"
                   FontWeight="{Binding FontWeight, ElementName=root}"
                   Foreground="{Binding ComputedForeground, ElementName=root}"
                   HorizontalAlignment="{Binding HorizontalAlignment, ElementName=root}"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>
